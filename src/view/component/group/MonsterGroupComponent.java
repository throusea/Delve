package view.component.group;

import model.race.Group;
import view.component.Bound;

public class MonsterGroupComponent extends RaceGroupComponent{
    public MonsterGroupComponent(Bound bound, Group group) {
        super(bound, group);
    }
}
